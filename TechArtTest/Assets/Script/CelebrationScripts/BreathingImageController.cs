using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BreathingImageController : MonoBehaviour
{
    public Vector3 tar = new Vector3(0.35f, 0.35f, 0.35f);
    public Vector3 asdf = new Vector3(0.45f, 0.45f, 0.45f);
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
